export default ({
  Vue,
  router,
  options,
  siteData
}) => {
  // router.beforeEach((to, from, next) => {
  //   console.log(router.options);

  //   if (to.path === '/' || to.path === '/textbook.html') {
  //     next();
  //   } else {
  //     if (router.options.auth) {
  //       next();
  //     } else {
  //       alert('접근할 수 없습니다. 캡틴 판교님께 연락하세요.');
  //     }
  //   }
  // })
}
