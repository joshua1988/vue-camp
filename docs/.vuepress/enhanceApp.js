export default ({
  Vue,
  router,
  options,
  siteData
}) => {
  // router.beforeEach((to, from, next) => {
    // console.log(router.options);

    // if (to.path === '/' || to.path === '/textbook.html') {
    //   next();
    // } else {
    //   if (router.options.auth) {
    //     next();
    //   } else {
    //     alert('등록된 수강생 아이디로만 접근할 수 있습니다.');
    //   }
    // }
  // })
}
